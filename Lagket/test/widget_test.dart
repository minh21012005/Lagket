import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:lagket/main.dart';

void main() {
  testWidgets('LagketApp smoke test', (WidgetTester tester) async {
    // This test is a placeholder — Firebase init requires a real device/emulator.
    expect(true, isTrue);
  });
}
